import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as EdgeCaseStories from './edge-case.stories'

<Meta title="Introduction" />

# Vis Labels

Vis Labels is a lightweight, performance-focused library for rendering large numbers of HTML labels with built-in intersection handling. It was created to support scenes with many thousands of overlapping labels while keeping the API small and the rendering model predictable.

It powers label rendering in  [🪐 Cosmograph](https://cosmograph.app), the fastest web-based network graph rendering framework.

## Why Vis Labels

- Zero-dependency runtime package.
- Built for scale, including large sets of intersecting labels.
- Built-in overlap resolution with weighted label priority.
- Stable visibility decisions to reduce flicker between frames.
- Reuses DOM nodes by label `id` instead of recreating them every draw.
- Uses cached and estimated label measurements to avoid unnecessary layout work.
- Supports plain text, custom CSS, and trusted HTML / multi-line labels.
- Works both as a batch renderer (`LabelRenderer`) and as individual labels (`VisLabel`).
- Optional style injection, exported stylesheet, click handling, and wheel event forwarding for interactive canvases and graphs.

## Live Example

This is a small animated swarm using the same intersection logic as the larger performance demo:

<Canvas of={EdgeCaseStories.SmallSwarm} />

## Quick Start

Install the package:

```bash
npm install @cosmograph/vis-labels
```

Create HTML div element and [render labels](?path=/story/quick-start--labels):

```javascript
import { LabelRenderer } from '@cosmograph/vis-labels'

const div = document.querySelector('#labels')
const renderer = new LabelRenderer(div)

renderer.setLabels([
  { id: 'monster', text: '👾', x: 100, y: 50, opacity: 1 },
  { id: 'alien', text: '👽', x: 50, y: 150, opacity: 1 },
  { id: 'ufo', text: '🛸', x: 150, y: 150, opacity: 1 },
])
renderer.draw()
```

Or create [single Vis label](?path=/story/quick-start--single-label):

```javascript
import { VisLabel } from '@cosmograph/vis-labels'
const div = document.querySelector('#labels')

const label = new VisLabel(div, '🐣')
label.setPosition(100, 110)
label.setVisibility(true)
label.setOpacity(1)
label.draw()
```

## Styling

By default, Vis Labels injects its base styles automatically. If you use `dontInjectStyles`, import the bundled stylesheet instead:

```javascript
import '@cosmograph/vis-labels/styles.css'
```

The label appearance can be customized with CSS variables, either globally on `:root` or on a specific labels container. See the [CSS variables story](?path=/story/quick-start--css-variables) for a live example.

Available variables:

- `--vis-label-background-color`: label background fill.
- `--vis-label-color`: label text color.
- `--vis-label-border`: label border.
- `--vis-label-box-shadow`: label shadow.
- `--vis-label-filter`: visual filter applied to each label.
- `--vis-label-border-radius`: label corner radius.
- `--vis-label-font-weight`: default text weight.
- `--vis-label-transition`: transition used for label state changes.
- `--vis-label-cursor`: cursor shown when hovering labels.
- `--vis-label-max-width`: maximum label width.
- `--vis-label-pointer-events`: default pointer events behavior for each label.

Example:

```css
#labels {
  --vis-label-background-color: rgba(118, 224, 196, 0.16);
  --vis-label-color: #d6fff4;
  --vis-label-border: 1px solid rgba(118, 224, 196, 0.35);
  --vis-label-box-shadow: 0 10px 18px rgba(0, 0, 0, 0.22);
  --vis-label-filter: drop-shadow(0 10px 18px rgba(0, 0, 0, 0.28));
  --vis-label-border-radius: 999px;
  --vis-label-font-weight: 700;
  --vis-label-transition: opacity 180ms ease, transform 180ms ease;
  --vis-label-cursor: pointer;
  --vis-label-max-width: 220px;
  --vis-label-pointer-events: auto;
}
```
